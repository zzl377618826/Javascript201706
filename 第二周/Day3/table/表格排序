1.获取初始数据 （ajax）
2. 数据绑定（字符串拼接 有多少个对象 拼接多少个tr 对象有多少个属性（不包含 pin） 拼接多少个td）
3.绑定排序事件 绑定给th （只给带有className === 'cursor'的th绑定事件）
排序问题：
拼音：根据存储在tr上的html属性（pin sex）进行localeCompare 排序
数字：当前点击th的那一列 每个tr所对应td的内容 进行排序

  （1）升降序切换 btn.flag *= -1
  （2）当前点击的不是这一


  列的 就让其他列升降序标识为初始值  btn.flag = -1

4.隔行变色
（1）在初始数据输出后 执行一次
（2）在每一次排序完成后 再执行一次

